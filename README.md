# 📚 Library Book Management System – Full Stack CRUD Application

---------------

## 📘 Introduction
The Library Book Management System is a full-stack web application developed to manage library books efficiently. The project demonstrates CRUD (Create, Read, Update, Delete) operations using a React frontend and an Express.js backend. Users can add, view, update, and delete book records through a clean and interactive interface.

---

## 🎨 Frontend
The frontend of the application is built using **React.js**. It provides a clean and elegant user interface for managing library books. Axios is used to send HTTP requests to the backend REST APIs, and the UI updates dynamically based on the responses received.

**Technologies Used:**
- React.js
- Axios
- HTML & CSS

---

## ⚙️ Backend
The backend is developed using **Node.js and Express.js**. It provides RESTful APIs to perform CRUD operations on book data. All validations and data processing are handled on the server side to ensure proper data management and integrity.

**Technologies Used:**
- Node.js
- Express.js
- REST API

---

## ▶️ Running the Application in Local Environment

### Backend Setup
```bash
npm install
node index.js
